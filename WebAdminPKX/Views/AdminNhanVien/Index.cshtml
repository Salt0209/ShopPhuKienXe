@model IEnumerable<Domain.Models.TblNhanVien>

@{
    ViewData["Title"] = "Index";
}
<p class="mb-3">
    <a class="btn btn-primary" asp-action="Create">Tạo mới</a>
</p>
<div class="container">
    <div class="row">
        <div class="col-12">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th scope="col">@Html.DisplayNameFor(model => model.IdNhanVien)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.SHoTen)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.SEmail)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.SSdt)</th>
                        <th scope="col">@Html.DisplayNameFor(model => model.IdQuyenHanNavigation.STenQuyenHan)</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@Html.DisplayFor(modelItem => item.IdNhanVien)</th>
                            <th scope="row">@Html.DisplayFor(modelItem => item.SHoTen)</th>
                            <td>@Html.DisplayFor(modelItem => item.SEmail)</td>
                            <td>@Html.DisplayFor(modelItem => item.SSdt)</td>
                            <td>@Html.DisplayFor(modelItem => item.IdQuyenHanNavigation.STenQuyenHan)</td>
                            <td>
                                <a class="btn btn-success" href="@Url.Action("Edit", "AdminNhanVien",new {id=item.IdQuyenHan})">
                                    <i class="far fa-edit"></i>
                                </a>
                                <a class="btn btn-danger" href="@Url.Action("Delete", "AdminNhanVien",new {id=item.IdQuyenHan})">
                                    <i class="far fa-trash-alt"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
